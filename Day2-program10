#include<iostream>
using namespace std;
int main()
{
	int n,temp,rev,sum=0;
	cout<<"enter the number:"<<endl;
	cin>>n;
	while(n!=0)
	{
		rev=n%10;
		sum=sum+rev;
		n=n/10;
	}
	cout<<"sum of numbers="<<sum;
	return 0;
}
